package model.tile;

import model.Position;

import java.awt.image.BufferedImage;

public class NormTiles extends Tiles {
    public NormTiles(BufferedImage img, Position pos, int w, int h) {
        super(img, pos, w, h);
    }
}
