package model.tile;

import java.awt.image.BufferedImage;

import model.Position;

public class Goal extends Tiles {

	public Goal(BufferedImage img, Position pos, int w, int h) {
		super(img, pos, w, h);
	}

}
